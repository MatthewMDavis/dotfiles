"Assign to neovim terminal for testing

let test#strategy = "neovim"

"Configure leader keys for vim-test plugin

nmap <silent> <leader>ts :TestNearest<CR>
nmap <silent> <leader>fs :TestFile<CR>
nmap <silent> <leader>as :TestSuite<CR>
nmap <silent> <leader>ls :TestLast<CR>
nmap <silent> <leader>gs :TestVisit<CR>
