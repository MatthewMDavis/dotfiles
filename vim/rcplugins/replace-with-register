" Replace With Register - Custom operator command for replacing
" this takes the place of visually selecting then pasting, e.g. `viw"ap`
" becomes `"agriw`.

Bundle "vim-scripts/ReplaceWithRegister"

nmap <leader>gr "*gr

" vim:ft=vim
