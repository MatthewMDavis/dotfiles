" Easy Buffer - Buffer list

nnoremap <leader>be :EasyBufferHorizontal<cr>

autocmd FileType easybuffer call s:EasyBufferSettings()

function! s:EasyBufferSettings()
  setl foldlevel=999
  nmap <buffer>q <Plug>Kwbd
endfunction

" vim:ft=vim
